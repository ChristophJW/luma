{
  "name": "guest",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Luma guest camera — deliberately tiny, web only",
  "scripts": {
    "dev": "vite --host",
    "build": "vite build",
    "preview": "vite preview --host",
    "typecheck": "tsc --noEmit",
    "size": "npm run build && node ../scripts/check-bundle-size.mjs"
  },
  "dependencies": {
    "@luma/tokens": "*",
    "preact": "^10.25.4"
  },
  "devDependencies": {
    "@preact/preset-vite": "^2.10.1",
    "typescript": "^5.7.3",
    "vite": "^6.0.11"
  }
}
